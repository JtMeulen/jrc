import { Meta, Source, Unstyled } from "@storybook/addon-docs";
import { version } from "../../package.json";

<Meta
  title="Docs/Introduction"
  parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Introduction

Joe React Components (JRC) is a collection of reusable React components that can be used to build user interfaces. It includes components such as buttons, forms, input fields, and more.

## Installation

To [install JRC](https://www.npmjs.com/package/joe-react-components) (latest version: <strong>{version}</strong>) in your project, you can use npm:

<Source
  dark
  language="shell"
  code={`npm install joe-react-components`}
/>

## Usage

To use a component from JRC, simply import it into your React application:

<Source
  dark
  language="js"
  code={`
  import { Button } from "joe-react-components";

class MyComponent extends React.Component {
  render() {
    return (
      <Button onClick={this.handleClick}>Click me</Button>;
    );
  }
}
  `}
/>

## Development

To develop and contribute to JRC, follow these steps:

1. Clone the repository:

<Source
  dark
  language="shell"
  code={`git clone https://github.com/JtMeulen/jrc.git`}
/>

2. Install the dependencies:

<Source
  dark
  language="shell"
  code={`npm install`}
/>

3. Start the Storybook development server:

<Source
  dark
  language="shell"
  code={`npm run start`}
/>

4. Make changes to the components in the src directory.

5. Build the components using Rollup:

<Source
  dark
  language="shell"
  code={`npm run build`}
/>

6. Publish the library to npm:

<Source
  dark
  language="shell"
  code={`npm publish`}
/>
