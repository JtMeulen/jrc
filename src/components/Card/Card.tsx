import React from "react";
import cn from "classnames";
import "./Card.scss";

export interface CardProps {
}

const Button = ({
}: CardProps) => {
  return (
    <div></div>
  );
};

export default Button;
